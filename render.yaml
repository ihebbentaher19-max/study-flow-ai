services:
  - type: web
    name: study-flow-ai
    runtime: node
    region: oregon
    plan: starter
    buildCommand: bun install && bun run build
    startCommand: node .output/server/index.mjs
    envVars:
      - key: NODE_VERSION
        value: 24.14.1
      - key: PORT
        value: 3000
